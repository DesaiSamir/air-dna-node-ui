
export type propertyFilterType = {
    minPrice: number,
    maxPrice: number,
    maxSelectedPrice: number,
    minBedrooms: number,
    maxBedrooms: number,
    minBathrooms: number,
    maxBathrooms: number,
    minOccupancy: number,
    maxOccupancy: number,
}